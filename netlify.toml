[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter @workspace/eew-website run build"
  publish = "artifacts/eew-website/dist/public"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
